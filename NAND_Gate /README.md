# 2-Input NAND Gate

Modeled universal logic using a continuous `assign` statement with bitwise AND (`&`) and NOT (`~`) operators. Verified logic output using EPWave waveform simulation.

## Simulation Waveform
![NAND Gate Waveform](waveform.png)
